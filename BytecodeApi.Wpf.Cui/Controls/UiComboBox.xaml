<ResourceDictionary
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:ui="http://schemas.bytecode77.com/2023/xaml/presentation"
	xmlns:ctrl="clr-namespace:BytecodeApi.Wpf.Cui.Controls">
	<Style TargetType="{x:Type ctrl:UiComboBox}">
		<Setter Property="Padding" Value="8,3" />
		<Setter Property="HorizontalContentAlignment" Value="Left" />
		<Setter Property="VerticalContentAlignment" Value="Center" />
		<Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
		<Setter Property="ScrollViewer.CanContentScroll" Value="True" />
		<Setter Property="Foreground" Value="Black" />
		<Setter Property="BorderThickness" Value="1" />
		<Setter Property="BorderBrush" Value="#bababa" />
		<Setter Property="Background" Value="White" />
		<Setter Property="FocusVisualStyle" Value="{x:Null}" />
		<Setter Property="ItemsPanel">
			<Setter.Value>
				<ItemsPanelTemplate>
					<VirtualizingStackPanel />
				</ItemsPanelTemplate>
			</Setter.Value>
		</Setter>
		<Setter Property="Template">
			<Setter.Value>
				<ControlTemplate TargetType="{x:Type ctrl:UiComboBox}">
					<Border Name="Border" BorderThickness="{TemplateBinding BorderThickness}" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" SnapsToDevicePixels="True">
						<Grid>
							<ToggleButton IsChecked="{Binding IsDropDownOpen, Mode=TwoWay, RelativeSource={RelativeSource TemplatedParent}}" ClickMode="Press" Focusable="False">
								<ToggleButton.Template>
									<ControlTemplate TargetType="{x:Type ToggleButton}">
										<Border Background="Transparent">
											<Path Name="Arrow" Data="M0,0 L0,2 L4,6 L8,2 L8,0 L4,4 z" HorizontalAlignment="Right" VerticalAlignment="Center" Margin="0,0,5,0" Fill="#505050" />
										</Border>
										<ControlTemplate.Triggers>
											<Trigger Property="IsEnabled" Value="False">
												<Setter TargetName="Arrow" Property="Fill" Value="#909090" />
											</Trigger>
										</ControlTemplate.Triggers>
									</ControlTemplate>
								</ToggleButton.Template>
							</ToggleButton>
							<Grid Margin="0,0,17,0">
								<ContentPresenter Name="Content" Content="{TemplateBinding SelectionBoxItem}" ContentTemplate="{TemplateBinding ItemTemplate}" ContentTemplateSelector="{TemplateBinding ItemTemplateSelector}" HorizontalAlignment="{TemplateBinding HorizontalContentAlignment}" VerticalAlignment="{TemplateBinding VerticalContentAlignment}" Margin="{TemplateBinding Padding}" TextElement.Foreground="{TemplateBinding Foreground}" IsHitTestVisible="False" RecognizesAccessKey="True" ui:TextBlockService.TextTrimming="CharacterEllipsis" />
								<Border Name="TextBoxBorder" BorderThickness="0,0,1,0" BorderBrush="Transparent" Visibility="Collapsed">
									<TextBox Name="PART_EditableTextBox" VerticalContentAlignment="Center" Padding="{TemplateBinding Padding, Converter={ui:ThicknessConverter SetRight, Then={ui:ThicknessConverter AddLeft}, ThenParameter={ui:Double -2}}}" BorderThickness="0" Foreground="{TemplateBinding Foreground}" Background="{TemplateBinding Background}" IsReadOnly="{TemplateBinding IsReadOnly}" Focusable="True">
										<TextBox.ContextMenu>
											<ContextMenu>
												<MenuItem Command="Undo">
													<MenuItem.Icon>
														<ctrl:UiSvgImage Source="/BytecodeApi.Wpf.Cui;component/Resources/Icons/Undo.svg" Stretch="None" />
													</MenuItem.Icon>
												</MenuItem>
												<MenuItem Command="Redo">
													<MenuItem.Icon>
														<ctrl:UiSvgImage Source="/BytecodeApi.Wpf.Cui;component/Resources/Icons/Redo.svg" Stretch="None" />
													</MenuItem.Icon>
												</MenuItem>
												<Separator />
												<MenuItem Command="Cut">
													<MenuItem.Icon>
														<ctrl:UiSvgImage Source="/BytecodeApi.Wpf.Cui;component/Resources/Icons/Cut.svg" Stretch="None" />
													</MenuItem.Icon>
												</MenuItem>
												<MenuItem Command="Copy">
													<MenuItem.Icon>
														<ctrl:UiSvgImage Source="/BytecodeApi.Wpf.Cui;component/Resources/Icons/Copy.svg" Stretch="None" />
													</MenuItem.Icon>
												</MenuItem>
												<MenuItem Command="Paste">
													<MenuItem.Icon>
														<ctrl:UiSvgImage Source="/BytecodeApi.Wpf.Cui;component/Resources/Icons/Paste.svg" Stretch="None" />
													</MenuItem.Icon>
												</MenuItem>
												<Separator />
												<MenuItem Command="SelectAll" />
											</ContextMenu>
										</TextBox.ContextMenu>
									</TextBox>
								</Border>
							</Grid>
							<Popup Name="PART_Popup" Placement="Bottom" HorizontalOffset="-1" VerticalOffset="1" MinWidth="{TemplateBinding ActualWidth, Converter={ui:MathConverter Add}, ConverterParameter={ui:Double 10}}" MaxHeight="{TemplateBinding MaxDropDownHeight}" AllowsTransparency="True" StaysOpen="True" IsOpen="{TemplateBinding IsDropDownOpen}" Focusable="False">
								<Border Margin="0,0,10,10" BorderThickness="1" BorderBrush="#bababa" Background="White" SnapsToDevicePixels="True">
									<Border.Effect>
										<DropShadowEffect BlurRadius="4" ShadowDepth="4" Opacity=".4" />
									</Border.Effect>
									<ScrollViewer CanContentScroll="False">
										<ItemsPresenter KeyboardNavigation.DirectionalNavigation="Contained" />
									</ScrollViewer>
								</Border>
							</Popup>
						</Grid>
					</Border>
					<ControlTemplate.Triggers>
						<Trigger Property="IsEditable" Value="True">
							<Setter Property="IsTabStop" Value="False" />
							<Setter TargetName="Content" Property="Visibility" Value="Collapsed" />
							<Setter TargetName="TextBoxBorder" Property="Visibility" Value="Visible" />
						</Trigger>
						<Trigger Property="IsKeyboardFocusWithin" Value="True">
							<Setter TargetName="Border" Property="BorderBrush" Value="#006bbe" />
						</Trigger>
						<Trigger Property="IsMouseOver" Value="True">
							<Setter TargetName="Border" Property="BorderBrush" Value="#006bbe" />
							<Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="#006bbe" />
							<Setter TargetName="Border" Property="Background" Value="#e0eef9" />
						</Trigger>
						<Trigger Property="IsDropDownOpen" Value="True">
							<Setter TargetName="Border" Property="BorderBrush" Value="#005499" />
							<Setter TargetName="TextBoxBorder" Property="BorderBrush" Value="#005499" />
							<Setter TargetName="Border" Property="Background" Value="#cce4f7" />
						</Trigger>
						<Trigger Property="IsEnabled" Value="False">
							<Setter TargetName="Content" Property="TextElement.Foreground" Value="#757575" />
							<Setter TargetName="PART_EditableTextBox" Property="Foreground" Value="#757575" />
							<Setter TargetName="Border" Property="BorderBrush" Value="#e9e9e9" />
							<Setter TargetName="Border" Property="Background" Value="#f9f9f9" />
						</Trigger>
					</ControlTemplate.Triggers>
				</ControlTemplate>
			</Setter.Value>
		</Setter>
	</Style>
</ResourceDictionary>